---
description: Investigate a focused architecture or technical-design question without creating a separate workflow artifact.
agent: build
---

Invocation arguments: `$ARGUMENTS`.

Load the `research` skill and investigate the supplied question in the current architecture or technical-design context. If it is not discoverable, read `~/.config/opencode/skills/research/SKILL.md`. Ask for the question if it is missing. Do not require or create separate planning artifacts, change accepted decisions, or advance the workflow.
