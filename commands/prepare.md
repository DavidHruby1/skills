---
description: Prepares Vue component scaffolding. Creates <script>, <template> and <style> tags with needed imports.
agent: build
argument-hint: <component path/name and requirements>
---

Create the Vue component requested in `$ARGUMENTS`.

Follow the project's existing conventions. Add only the required imports and minimal `<script>`, `<template>`, and `<style>` scaffolding. Do not add tests, documentation, dependencies, or unrelated abstractions.
