---
description: Publish or update explicitly scoped PRs using the create-pr skill.
agent: build
---

Invocation arguments: `$ARGUMENTS`.

Load the `create-pr` skill and follow its scope and publication contract. If it is not discoverable, read `~/.config/opencode/skills/create-pr/SKILL.md`. Do not infer task-wide publication from a single-branch request or run implementation and review as part of direct publication.
