---
description: Alias for the exact implement workflow, with the same explicit task or plan arguments.
agent: build
---

Invocation arguments: `$ARGUMENTS`.

Read `/home/hruby/.config/opencode/commands/implement.md` in full and execute that contract with these exact invocation arguments. Do not maintain or invent a second workflow. If the contract is unavailable, report the blocker and stop.
