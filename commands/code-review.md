---
description: Review a branch, PR, WIP, or task using parallel Standards and Spec inspector reviews.
agent: build
---

Invocation arguments: `$ARGUMENTS`.

Load the `code-review` skill and follow it for the supplied scope. If it is not discoverable, read `~/.config/opencode/skills/code-review/SKILL.md`. Do not substitute manager-only review, implement fixes, or publish PRs.
